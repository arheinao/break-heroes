# Supabase (for email signup)
# Create a project at https://supabase.com and add these keys.
# The anon key is public-safe as long as Row Level Security is properly configured.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
